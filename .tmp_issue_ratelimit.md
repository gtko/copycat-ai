## 🚨 VULNÉRABILITÉ CRITIQUE

### Description
Aucun rate limiting n'est implémenté, permettant des attaques par force brute et l'abus des API coûteuses (Kimi, Stripe).

### Localisation
- Toutes les routes API
- Particulièrement `/api/generate` (appel IA coûteux)
- `/api/auth/login` (force brute magic links)
- `/api/stripe/checkout` (création abusive de sessions)

### Scénarios d'attaque

**1. Coûts API explosifs**
```bash
while true; do
  curl -X POST /api/generate -H "Cookie: session=xxx" -d '{...}'
done
```
→ Facture Kimi de milliers d'euros

**2. Force brute de sessions**
```bash
# Brute force des IDs de session (UUID mais quand même)
for id in $(cat wordlist.txt); do
  curl /api/plans -H "Cookie: session=$id"
done
```

**3. Spam de création de comptes**

### Impact
- Coûts astronomiques (API IA, Stripe)
- Déni de service
- Spam

### Solution recommandée
1. Rate limiting par IP: 100 req/min
2. Rate limiting par user: 10 generations/heure
3. Rate limiting auth: 5 tentatives/min

Avec Cloudflare KV ou un middleware:
```typescript
import { rateLimiter } from 'hono-rate-limiter';

app.use('/api/generate', rateLimiter({
  windowMs: 60 * 60 * 1000, // 1h
  max: 10,
  keyGenerator: (c) => c.get('user').id
}));
```

### Priorité
**P0 - Bloquant Production**

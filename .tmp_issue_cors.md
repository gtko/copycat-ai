## 🚨 VULNÉRABILITÉ CRITIQUE

### Description
La politique CORS est configurée avec `origin: '*'`, permettant à n'importe quel site web de faire des requêtes authentifiées vers l'API.

### Localisation
- `src/index.ts` ligne ~25

### Code vulnérable
```typescript
app.use('*', cors({ 
  origin: '*', 
  allowMethods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'] 
}));
```

### Scénario d'attaque
1. Victime connectée sur copycat-ai.com
2. Victime visite evil.com
3. evil.com fait une requête AJAX vers copycat-ai.com avec les cookies de session
4. L'API répond car CORS autorise '*'
5. evil.com récupère les données du business plan

### Impact
- CSRF-like attacks
- Exfiltration de données sensibles
- Actions non autorisées au nom de l'utilisateur

### Solution recommandée
```typescript
app.use('*', cors({ 
  origin: [c.env.APP_URL, 'https://copycat-ai.com'],
  allowMethods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
  credentials: true
}));
```

### Priorité
**P0 - Bloquant Production**
